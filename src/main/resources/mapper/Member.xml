<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">

    <select id="Login" parameterType="GwakMemberDto" resultType="Int">
        SELECT COUNT(*)
        FROM USER
        WHERE mem_id = #{memId}
          AND mem_pwd = #{memPwd}
    </select>

    <select id="MemberLogin" resultType="com.bitc.springteamproject1209.dto.MemberDto">
        Select * from tblmember where mem_email=#{memEmail} and mem_pwd=#{memPwd}
    </select>
</mapper>